name: MediVerify CI/CD Pipeline

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

env:
  PYTHON_VERSION: "3.10"
  NODE_VERSION: "20"

jobs:
  backend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest httpx pytest-asyncio
        
    - name: Run Backend Tests
      run: pytest tests/ -v
      env:
        SUPABASE_URL: "https://mock.supabase.co"
        SUPABASE_SERVICE_KEY: "mock_key"
        SECRET_KEY: "test_secret_key"

  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: './frontend/package-lock.json'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build Frontend
      run: npm run build
      
    - name: Lint
      run: npm run lint
      continue-on-error: true # Not blocking build on lint for now

  # Placeholder for deployment job, commented out until hosting is setup
  # deploy:
  #   needs: [backend-test, frontend-build]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy to Render/Vercel
  #       run: echo "Deployment steps go here"
